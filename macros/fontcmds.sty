% to be included into NFSS2 some day
% 
% We have already seen some examples with commands having arguments and
% changing font attributes. Using such commands instead of the
% declarative forms has the advantage of being consistent with all other
% structures defined by \LaTeX. We suggest to define the following
% commands for this purpose:
%    \begin{macrocode}
\newcommand{\textrm}[1]{{\rm#1}}
\newcommand{\textsf}[1]{{\sf#1}}
\newcommand{\texttt}[1]{{\tt#1}}
%    \end{macrocode}
%  for the series attribute:
%    \begin{macrocode}
\newcommand{\textbf}[1]{{\bf#1}}
\newcommand{\textmedium}[1]{{\mediumseries#1}}
%    \end{macrocode}
%  and for the shapes:
%    \begin{macrocode}
\newcommand{\textit}[1]{{\it#1\/}}
\newcommand{\textsl}[1]{{\sl#1\/}}
\newcommand{\textsc}[1]{\switch@slant{\sc#1}}
\newcommand{\textnormal}[1]{\switch@slant{\normalshape#1}}
%    \end{macrocode}
% 
% These commands are defined in the style option \styleoption{fontcmds}
% which can be loaded in the |\documentstyle| command. This style option
% also contains the command |\textem| which replaces the |\em| font change
% declaration of \LaTeX. Its definition is
%    \begin{macrocode}
\newcommand{\textem}[1]{\switch@slant{\em#1\/}}
\newcommand{\nospace}{\hspace{0pt}}
%    \end{macrocode}
%
%

%  \begin{macro}{\switch@slant}
%    undoc
%    \begin{macrocode}
\def\switch@slant{%
  \ifhmode
    \ifdim\lastskip>\z@
      \skip@\lastskip
      \unskip\/\hskip\skip@
    \fi
  \fi
}
%    \end{macrocode}
%  \end{macro}

